name: ci  build and push docker image to ECR

on:
  push:

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout code
          uses: actions/checkout@v4

        - name: Configure AWS credentials
          run: |
            aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
            aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            aws configure set default.region ${{ secrets.AWS_REGION }}

        - name: Login to Amazon ECR
          run: |
            aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 696035274327.dkr.ecr.us-east-1.amazonaws.com
        
        - name: Build and push Docker image to ECR
          run: |
            docker build -t medplus:v3 .
            docker tag medplus:v2 696035274327.dkr.ecr.us-east-1.amazonaws.com/medplus:v3
            docker push 696035274327.dkr.ecr.us-east-1.amazonaws.com/medplus:v3
        

  deploy:
    runs-on: ubuntu-latest
    needs: build-push
    steps: 
      - name: check my ssh key
        run: |
          echo "${{ secrets.SSH_KEY }}" > mykey.pem
          chmod 600 mykey.pem
          cat mykey.pem

          ssh -o StrictHostKeyChecking=no -i mykey.pem ubuntu@18.206.235.27 "docker pull 696035274327.dkr.ecr.us-east-1.amazonaws.com/medplus:v3"

                    ssh -o StrictHostKeyChecking=no -i mykey.pem ubuntu@18.206.235.27 "docker rm -f medplus"

          ssh -o StrictHostKeyChecking=no -i mykey.pem ubuntu@18.206.235.27 "docker run -d --name medplus -p 80:80 696035274327.dkr.ecr.us-east-1.amazonaws.com/medplus:v3"

























      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}
      # -
      #   name: Set up QEMU
      #   uses: docker/setup-qemu-action@v3
      # -
      #   name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v3
      # -
      #   name: Build and push
      #   uses: docker/build-push-action@v6
      #   with:
      #     push: true
      #     tags: smartagbawo/mediplus:v3
